local hook added for curl to remote jenkins build trigger
again
now added persmission to the folder so that jenkins can use that
agag
directory under root/ were denied even after setting the permission, so manually adding files to /var/www/html/filder
